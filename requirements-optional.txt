# =====================================================================
# requirements-optional.txt —— 可选依赖（站点热力地图）
# 默认需求已满足，地图为可选增强；如需要再单独安装：
#   pip install -r requirements-optional.txt
# =====================================================================

# folium：站点地图（未随核心环境安装，可选）
folium